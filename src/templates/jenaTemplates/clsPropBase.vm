#set($range = $prop.getRangeJava())
#set($rangeFull = $prop.getRangeJavaFull())    

/**
 * Domain property $propname
 * with uri $prop.getMapUri()
 */
public boolean exists$propnameCaps() {
	return hasProperty(${pkgBase}.${pkgTools}.${vocabName}.${propname});
}
	
public boolean has$propnameCaps($rangeFull #mixedlowercase($range)Value) {
	#if($p.isObjectProperty)
		return hasProperty(${pkgBase}.${pkgTools}.${vocabName}.${propname}, #mixedlowercase($range)Value);
	#else
		return hasLiteral(${pkgBase}.${pkgTools}.${vocabName}.${propname}, #mixedlowercase($range)Value);
	#end	
}

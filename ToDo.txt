Templates:
- hasProperty($range value) for each property (see clsPropBase.vm)

In Templates:
- MaxCardinality Constraints

JavaDocs:
 - These are completely missing
 
XsdTypes currently ignored:
- Entity

Misc:
- add support for primitive types?
- add helper class for XsdDuration (to create it?)
- How about OCL as query language?
- Count via Sparql?
- List as LazyProxyList with loadOnDemand?
